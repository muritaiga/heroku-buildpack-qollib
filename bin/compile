#!/usr/bin/env bash

indent() {
  sed -u 's/^/       /'
}

echo "-----> Install RM20BspLibTb"
BUILD_DIR=$1

echo "--> ENV RECEIVE_DATA =" $RECEIVE_DATA | indent
echo $RECEIVE_DATA >> env.json
cat env.json | jq .push_metadata.env.QUOTAGUARDSTATIC_URL
cat env.json | jq .push_metadata.env.QOLLIBS_URL

VENDOR_DIR="vendor"
LIBRARY_DIR="qollib"
DOWNLOAD_URL="https://s3.amazonaws.com/qolpilotlibs/RM20BspLibTb"
PROXY_URL="http://quotaguard2622:9b0e4c58cdc6@us-east-1-static-brooks.quotaguard.com:9293"
env
echo "DOWNLOAD_URL = " $DOWNLOAD_URL | indent
echo "PROXY_URL" $PROXY_URL | indent

cd $BUILD_DIR
mkdir -p $VENDOR_DIR
cd $VENDOR_DIR
mkdir -p $LIBRARY_DIR
cd $LIBRARY_DIR
curl --proxy $PROXY_URL $DOWNLOAD_URL -O
chmod +x RM20BspLibTb

echo "exporting PATH and LIBRARY_PATH" | indent
PROFILE_PATH="$BUILD_DIR/.profile.d/qollib.sh"
mkdir -p $(dirname $PROFILE_PATH)
echo 'export PATH="$PATH:$HOME/vendor/qollib"' >> $PROFILE_PATH
echo 'export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:$HOME/vendor/qollib"' >> $PROFILE_PATH

