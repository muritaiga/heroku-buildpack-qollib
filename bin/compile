#!/usr/bin/env bash

indent() {
  sed -u 's/^/       /'
}

echo "-----> Install RM20BspLibTb"
BUILD_DIR=$1
VENDOR_DIR="vendor"

env
echo "DOWNLOAD_URL = " $QOLLIBS_URL | indent

cd $BUILD_DIR
mkdir -p $VENDOR_DIR
cd $VENDOR_DIR
echo "PROXY_URL" $QUOTAGUARDSTATIC_URL | indent
curl --proxy $QUOTAGUARDSTATIC_URL $QOLLIBS_URL -O
chmod +x RM20BspLibTb

echo "exporting PATH and LIBRARY_PATH" | indent
PROFILE_PATH="$BUILD_DIR/.profile.d/qollib.sh"
mkdir -p $(dirname $PROFILE_PATH)
echo 'export PATH="$PATH:$HOME/vendor/qollib"' >> $PROFILE_PATH
echo 'export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:$HOME/vendor/qollib"' >> $PROFILE_PATH

